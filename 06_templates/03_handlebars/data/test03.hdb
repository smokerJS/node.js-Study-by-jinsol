<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title>{{title}}</title>
    </head>
    <body>
        <div id="result">
            <h2>{{title}}</h2>
            <div>
                <h2>{{body.title}}</h2>
                <h2>{{body.content}}</h2>
            </div>
        </div>
    </body>
</html>