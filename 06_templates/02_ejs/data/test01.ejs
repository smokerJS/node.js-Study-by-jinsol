<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title>EJS 사용하기</title>
    </head>
    <body>
        <h2>ejs 기본문법</h2>
        <table>
            <thead>
                <tr>
                    <th>번호</th>
                    <th>제목</th>
                    <th>작성자</th>
                </tr>
            </thead>
            <tbody>
                <% for(let i = 0 ; i < 5 ; i++){ %>
                <tr>
                    <td><%= i %></td>
                    <td><%= i %></td>
                    <td><%= i %></td>
                </tr>
                <%}%>
            </tbody>
        </table>
    </body>
</html>