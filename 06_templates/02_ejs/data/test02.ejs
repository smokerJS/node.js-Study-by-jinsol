<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title><%= title %></title>
    </head>
    <body>
        <h2><%= title %></h2>
        <table>
            <thead>
                <tr>
                    <th>번호</th>
                    <th>제목</th>
                    <th>작성자</th>
                </tr>
            </thead>
            <tbody>
                <% for(let i = 0 ; i < arr.length ; i++){ %>
                <tr>
                    <td><%= arr[i] %></td>
                    <td><%= arr[i] %></td>
                    <td><%= arr[i] %></td>
                </tr>
                <%}%>
            </tbody>
        </table>
    </body>
</html>